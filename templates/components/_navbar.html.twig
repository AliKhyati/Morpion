<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="d-flex justify-content-start">
        <a  class="me-2 nav-link" href="{{ path('home') }}">Accueil</a>
        <a class="nav-link" href="{{ path('home') }}">Classement</a>
    </div>
</nav>